The user wants to check their Skills Exchange inbox for skills that others have sent them.

Run this command and show the output:

```bash
skillshare inbox
```

If there are skills in the inbox, show them clearly. For each skill, explain that the user can install it by running:

```bash
skillshare add <id>
```

Or by using the `/skills-add <id>` command inside Claude Code.

If the inbox is empty, encourage the user to share their username so others can find and send them skills.
