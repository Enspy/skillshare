The user wants to look up another Claude Code user's profile on Skills Exchange.

Run this command, using the username the user provided:

```bash
skillshare who $ARGUMENTS
```

Show the output. If the user has skills listed, present them clearly. If they want to send a skill to that user, suggest:

```bash
skillshare send @username /skillname
```
